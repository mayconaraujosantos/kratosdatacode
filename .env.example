# Arquivo de exemplo - NÃO commitar o .env real
# Copie este arquivo para .env e configure as variáveis necessárias

# Railway automaticamente define PORT, mas você pode definir um padrão local
PORT=3000

# Configurações de build do React
GENERATE_SOURCEMAP=false
REACT_APP_BUILD_PATH=./build

# Configurações para produção
NODE_ENV=production